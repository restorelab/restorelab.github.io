---
import Landing from '../layouts/Landing.astro';
import { en } from '../i18n/landing';

/**
 * La landing anglaise, servie à la racine. L'anglais est la locale `root` de
 * Starlight : il n'a pas de préfixe d'URL.
 */
---

<Landing copy={en} lang="en" />
